{
  "name": "pdf_gen",
  "version": "0.0.1",
  "description": "socket.io app",
  "dependencies": {
    "express": "^4.17.1",
    "pdfkit": "^0.10.0",
    "shortid": "^2.2.15"
  },
  "devDependencies": {
    "nodemon": "^1.19.3"
  },
  "scripts": {
    "start": "node src/index.js",
    "live": "nodemon src/index.js",
    "build": "rm -rf static/site/* && cd ../pdf_frontend/ && npm run build && cp -r build/* ../pdf_backend/static/site"
  }
}
